import { MytagDirective } from './mytag.directive';

describe('MytagDirective', () => {
  it('should create an instance', () => {
    const directive = new MytagDirective();
    expect(directive).toBeTruthy();
  });
});
